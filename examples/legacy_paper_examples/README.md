## Legacy Paper Examples

This folder contains older example scripts, plotting scripts, and CSV launch files
from the original t-NN paper-style experiments. They are kept for reference.

The MNIST VarPro autoencoder report workflow lives in `../mnist/`.
